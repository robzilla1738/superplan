# Phase spec template - write to {{RUN_ROOT}}/phases/phase-N.md
# ({{RUN_ROOT}} is this run's namespaced dir, e.g. .supergoal/add-dark-mode-Ab3Kx9.)
#
# The executing agent reads this file for human intent, but the source of
# truth is {{RUN_ROOT}}/run.json. Keep this phase spec aligned with the
# matching phase object in run.json.
#
# Replace {{...}} placeholders. Keep the SUPERGOAL_PHASE_START block
# verbatim so validate-phase.sh and the runtime can anchor on it.

SUPERGOAL_PHASE_START
Phase: {{N}} of {{TOTAL}} - {{PHASE_NAME}}
Phase id: {{N}}
Task: {{ONE_LINE_TASK}}
Type: {{TAGS}}
Allowed paths: {{ALLOWED_PATHS_CSV}}
Mandatory command ids: {{COMMAND_IDS_CSV}}
Acceptance criteria: {{CRIT_COUNT}}
Trust-prior criteria: {{TRUST_PRIOR_COUNT}}
Evidence required: {{EVIDENCE_CSV}}
Depends on phases: {{DEPS}}
Run root: {{RUN_ROOT}}

## Kernel contract

- Canonical phase object: `{{RUN_ROOT}}/run.json` -> `phases[{{N}}]`
- Evidence root: `{{RUN_ROOT}}/evidence/phase-{{N}}/`
- Command logs: `{{RUN_ROOT}}/evidence/phase-{{N}}/commands/<command-id>.log`
- Gate command: `python "{{RUN_ROOT}}/sg.py" gate-phase "{{RUN_ROOT}}" {{N}}`

## Why

{{WHY_ONE_SENTENCE}}

## Work

- {{WORK_BULLET_1}}
- {{WORK_BULLET_2}}
- ...

## Allowed paths

The phase may edit only these paths unless the gate-facing run manifest is updated before work continues:

- {{ALLOWED_PATH_1}}
- {{ALLOWED_PATH_2}}
- ...

## Acceptance criteria

Each criterion must include its verification class. Use:

- `mechanical` for checks a script/command/file inspection can verify.
- `human` for subjective UI/content judgment that must be honestly labeled.
- `trust-prior` only when the audit cannot re-check it mechanically.

Criteria:

- [{{VERIFICATION_1}}] {{CRITERION_1}} (evidence: {{CRITERION_1_EVIDENCE}})
- [{{VERIFICATION_2}}] {{CRITERION_2}} (evidence: {{CRITERION_2_EVIDENCE}})
- ...

## Mandatory commands

Run each command, save its log under `evidence/phase-{{N}}/commands/`, and include an explicit exit marker such as `exit 0`.

- `{{COMMAND_ID_1}}`: `{{COMMAND_1}}`
- `{{COMMAND_ID_2}}`: `{{COMMAND_2}}`
- ...

## Evidence required

These paths are checked by `sg.py gate-phase` before `SUPERGOAL_PHASE_DONE` is allowed:

- {{EVIDENCE_FILE_1}}
- {{EVIDENCE_FILE_2}}
- ...

## Notes

{{ANY_PHASE_SPECIFIC_GUIDANCE}}

---

During execution, print SUPERGOAL_PHASE_START, do the work, print
SUPERGOAL_PHASE_VERIFY, run the gate command above, print MEMORY_SAVED,
then print SUPERGOAL_PHASE_DONE only if the gate succeeds. On failure,
follow the recovery protocol in {{RUN_ROOT}}/PROTOCOL.md.
